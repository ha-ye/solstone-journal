# Contract Fixtures

These are faithful synthetics standing in for the real suze tmux
`chronicle/*/observer/failed/*screen.jsonl` artifact, which could not be pulled
from production safely.

The tmux observer shape has a header with no `raw` because there is no source
media. Its records carry `timestamp`, matching the screen record floor.
